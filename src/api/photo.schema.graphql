type Photo {
  filename: String
  description: String
  isPrivate: Boolean
  createdByUser: User
  createdAt: GraphQLDateTime
  species = [Species]
}

type Query {
  photos: [Photo!]
  photo(id: ID!) : Photo
}

type Mutation  {

}